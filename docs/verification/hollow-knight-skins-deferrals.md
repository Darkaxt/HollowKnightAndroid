# Hollow Knight skin-storage deferrals

The H4 quota-admission MVP enforces bounded, fail-closed reservations. It deliberately does not choose retention roots, traverse a retention graph, or delete storage.

| Stable ID | Status | Exact missing behavior | Reason | Dependency | Named target milestone | Acceptance condition / test | Why admission-only remains safe and nonblocking |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `H4-STORAGE-RETENTION-001` | `DEFERRED` | Select and retain the last eight registry generations and eight highest CLOSED sessions while also retaining the active lease and every generation, descriptor, object, and import receipt named by `current`/`previous`/`next`, active visual receipts, non-clear interlocks, and retained sessions. Build the complete visual/interlock/session pin graph. Treat explicit generation pins as non-transitive: retain each explicitly named generation and its direct object/import-receipt dependencies, but do not retain older ancestors solely through parent edges. | The time-boxed MVP requires mandatory admission limits now; implementing pin selection without the later import and production-launch ownership boundaries would risk inventing authority or deleting live rollback evidence. | Approved import-publication ownership contract and isolated host seam; approved acquisition/session ownership contract and isolated host seam; exact registry/descriptor dependency-record schemas. Production callers are not prerequisites and remain disabled. | `H4 post-MVP storage hardening before production launcher acquisition/import wiring` | Host tests construct more than eight generations and CLOSED sessions plus out-of-window visual, interlock, active/retained-session, and explicit generation pins; the retained set is deterministic, includes every direct dependency, excludes unpinned history below the floor, and proves explicit generation pins do not traverse parent history transitively. Active and UNKNOWN lease cases remain mutation-blocking. | Admission counts the entire exact profile and sessions trees, includes concurrent reservations, and rejects with `PROFILE_QUOTA_EXCEEDED` before any quota-governed publication mutation. It never evicts or mutates existing evidence, so missing retention can cause a conservative capacity rejection but cannot remove a pin, corrupt recovery, or block the later isolated ownership-seam work. |
| `H4-STORAGE-GC-002` | `DEFERRED` | Implement bounded garbage collection only for contained, no-alias, unpinned state: gate deletion on no active or UNKNOWN lease; protect every retained generation/descriptor/object/import-receipt dependency; preserve parent durability barriers; fail closed on corrupt/ambiguous evidence; cover failures and restart at every delete and parent-sync boundary; and bound, validate, resume, and clean tombstones without crossing the exact Hollow Knight skins root. | Safe deletion depends on the completed retention authority and final publication/lease ownership. A partial collector is less safe than conservative admission rejection. | `H4-STORAGE-RETENTION-001`; approved contained-cleanup ownership contract; reusable durable deletion/tombstone fault seams. Production import/acquisition callers remain disabled until this milestone closes. | `H4 post-MVP storage hardening before production launcher acquisition/import wiring` | Fault/restart tests prove active and UNKNOWN leases perform zero deletion; aliases, out-of-root paths, corrupt nodes, pins, and dependencies are never traversed or deleted; each successful contained delete is followed by the required parent barrier; every interruption converges safely; tombstone scanning/cleanup is bounded; and only verified unpinned generations, CLOSED-session orphans beyond retention, and their unreferenced dependencies are collectible. | The admission-only MVP has no collector and performs quota accounting through a bounded, no-follow iterator. Ordinary quarantine, registry, and acquisition reservations cannot consume the fixed 3,555,328-byte cumulative lifecycle margin in either cap. That pool conservatively sums one full fixed claim budget, one full fixed close budget, and two full fixed recovery budgets; only those fixed lifecycle classes may consume it, with shared-ledger concurrency checks. For growth admitted by this authority within those fixed budgets, an ordinary-ceiling pending acquisition therefore retains capacity for claim, recovery that may durably repair or close state, close, and a subsequent full recovery. Reconstructed real quota adapters share pending-cleanup ownership by exact physical root, so cleanup is retried or rebased before later admission. External pre-existing overfill, lifecycle growth beyond its fixed budgets, unsafe filesystem evidence, or actual device free-space exhaustion can still fail closed; the cumulative margin does not guarantee capacity against those external conditions. |
