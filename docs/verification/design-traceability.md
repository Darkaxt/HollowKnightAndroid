# Unified Platform Design Traceability

Last cross-check: 2026-09-01, after the first signed Hollow Knight H2
lower-companion matrix and the host-green correction batch, while retaining the
Hollow Knight HUD/pages/Mods/skins-first order.

States follow the design contract: `COMPLETE`, `IN-PROGRESS`, `BLOCKER`,
`DEFERRED`, and `NOT-STARTED`. `NOT-STARTED` means the planned milestone has not begun. The
The final fork package is the only matching Dual Souls/Hollow Knight package on
the Thor and retains current generations for both profiles. Signed candidate
`c2eb819` updated it in place without changing the UID or first-install
timestamp and published exact Hollow Knight generation
`gen-be61fa68-e065-4cdb-9972-d595c93a7698`. Its lower-companion matrix proves
live HUD routing, clean primary HUD, lower page switching and selection,
blurred/dimmed backdrop, pause presentation, and teardown. Resident TMP tab
text and remaining controlled/static-state H2 rows are still blockers; Mods,
skins, shared extraction, Silksong adaptation, and the physical
detach/single-display deferral remain open.

| Requirement | Planned milestone | State | Evidence or gap | Dependency | Acceptance test |
|---|---:|---|---|---|---|
| Host test harness: JUnit, Robolectric, Android resources, and patch compile check | 1 | COMPLETE | `docs/verification/host-poc-2026-08-30.md` records green Gradle and 32-source patch checks | Pinned Unity Android module and user-owned Silksong assemblies | Repeat the recorded Gradle command and `check.ps1` successfully |
| Evidence: Silksong Linux layout and Vulkan slices | Baseline | COMPLETE | Design evidence records Unity 6000.0.50f1 and 2,068 Addressables bundles | User-owned Linux source | Repeat census when registering a new source build |
| Evidence: Hollow Knight 1.5.12612 backward-compatibility oracle | Baseline | COMPLETE | Design evidence records 501 levels, 501 shared assets, and 503 parsed serialized files; this version is not a current implementation target | User-owned compatibility source | Use only to compare observable compatibility after validating 1.5.12620 |
| Evidence: Hollow Knight 1.5.12612 compatibility Vulkan coverage | Baseline | COMPLETE | Design evidence records 125 of 125 shaders with Vulkan slices only as a compatibility oracle, not current-source acceptance | User-owned compatibility source | Compare after 1.5.12620 `manifest-classic-tree` reports zero missing Vulkan shaders |
| Evidence: Windows Hollow Knight is unsuitable | Baseline | COMPLETE | Design evidence records Direct3D 11 and no Vulkan slices | None | Validator returns `WINDOWS_SOURCE` for a synthetic Windows fixture |
| Evidence: production ABI is ARM64 only | Baseline | COMPLETE | Design and repository build configuration both specify `arm64-v8a` | None | APK/native artifact ABI inspection at release |
| Evidence: x86-64 emulator cannot prove the native pipeline | Baseline | COMPLETE | Design records the ABI mismatch and limits emulator evidence to fakes | None | No release row cites x86-64 as native/device proof |
| Current Hollow Knight 1.5.12620 source acceptance | 2 | COMPLETE | Exact real-source report proves revision 12620, Unity 6000.0.61f1, Linux target, 1,748 manifest files, 125 shaders, zero missing Vulkan, and zero diagnostics; Kotlin returns `SUPPORTED` | None for source acceptance | Repeat environment-driven real-source integration test against semantic manifest hash `8035cad2…f5074f` |
| Goal 1: one launcher APK and package identity | 4 | BLOCKER | The shared launcher picker and both routes exist. Hollow Knight and fork-signed Silksong each reach gameplay through this codebase, and the final package UI identifies both profiles. The current final-package installation contains only the Silksong generation while the preserved Hollow Knight proof remains in the non-colliding PoC package | Controlled Hollow Knight adoption into the final package and Task 11 switching | Both profiles launch from one installed `io.github.darkaxt.dualsouls` package |
| Goal 2: choose game before Unity | 4 | BLOCKER | Both choices render before Unity and each selected profile has separately launched its ARM64 Unity process. Both cold-switch directions inside one final-package installation remain unproven | Hollow Knight final-package adoption and Task 11 device switching | Both ARM64 switch directions prove the prior process is gone before the next starts |
| Goal 3: independent provision/validate/build/repair/reset/launch | 3–4 | BLOCKER | Both exact profiles have separately completed production conversion, verified atomic publication and gameplay. Full same-installation preservation/reset/switch isolation remains absent | Hollow Knight final-package adoption, then Task 11 | Isolation and reset matrix plus real provisioning pass for both profiles |
| Goal 4: reuse on-device Vulkan architecture | 3 | COMPLETE | Attempt 4 loads the exact 6000.0.61f1 Android `libunity`, `libmain`, and generated AArch64 `libil2cpp.so`, mounts the ZIP32 base plus main OBB, initializes Vulkan/audio, logs the injection probe, and reaches a user-confirmed playable room | None for the Hollow Knight architecture gate | Repeat exact generation launch to the first room through the shared player |
| Goal 5: classic Hollow Knight converter | 2 | COMPLETE | The exact 1.5.12620 tree converted 1,005 serialized files, preserved 610 sidecars, excluded four desktop plugins, reported all 125 shaders Vulkan-capable, reopened outputs with zero diagnostics, and retained source-tree hash `1319a0ea…daa42f8` | None for converter correctness; device consumption remains Goal 4 | Repeat real-source inventory/conversion and zero-diagnostic reopen |
| Goal 6: shared mod and skin libraries with adapters | H3–H6, S2 | BLOCKER | The parent BepInEx/Harmony weaver, shared external DLL discovery, content stamps, toggle gates, selected-profile IL2CPP wiring, and mod UI are merged with host coverage. The corrected order first ports the existing Hollow Knight Mods and skin behavior, proves it, then extracts shared contracts and adds Silksong | H3–H6 before S2 | Hollow Knight Mods/skins pass first; extracted discovery, compatibility, dependency, safe-mode, scanner, and adapter tests then pass for both profiles |
| Goal 7: multiple skins and death rotation | H4–H6, S2 | BLOCKER | The pure shared rotation state machine passes 6 death/repeated-death/stable-respawn/disabled/single-pack/apply-failure tests. Hollow Knight's real scanner/application/lifecycle path is now the first implementation target; Silksong follows after shared extraction | H4/H5 before H6/S2 | Hollow Knight death selects and stable respawn applies with rollback before the same shared contract is accepted for Silksong |
| Goal 8: shared dual-screen Vulkan foundation | H1–H6 | IN-PROGRESS | H1 isolates the transport and H2 now runs the pinned Hollow Knight companion through it. The lower-HUD-only signed matrix proves live routing, primary cleanup, backdrop, lower-controller page selection, item details/prompt, pause presentation, and teardown | Close H2's label/static-state/lifecycle rows, H3–H5 including the tracked physical detach/single-display gate, then H6 extraction | Hollow Knight HUD/pages/Mods/skins pass on Thor; shared extraction preserves that matrix; Silksong transport regression remains green |
| Goal 11: faithful Dual Souls bottom-screen port | H2–H6, S1–S2 | IN-PROGRESS | H2 is source-complete and device-partial on the unchanged pinned Dual Souls composition. The authored Silksong shell remains `REJECTED_PROTOTYPE`; preserved Silksong source work stays parked until the Hollow Knight reference and shared contracts pass | Close H2, then H3–H6 before S1–S2 of `2026-09-01-hollow-knight-first-dual-souls.md` | First prove the unchanged Hollow Knight composition/behavior; then side-by-side Thor review proves the same navigation, modal, lifecycle, fallback, accessibility, localization, OLED, persistence, Mods, and skin behavior with Silksong content |
| Launcher supplied-art redesign and per-game direct shortcuts | 4 | DEFERRED | The supplied SteamGridDB grids, heroes, logos, and individual icons are preserved with attribution. The Hollow Knight first-boot dependency is complete; redesign and Android pinned shortcuts remain scheduled in Task 11, and image generation is forbidden | Task 11 launcher implementation | Both cards use preserved assets and each shortcut launches only its exact ready profile through the shared eligibility path |
| Goal 9: exclude proprietary inputs/artifacts | Every stage | COMPLETE | The post-parent-sync boundary scan finds zero forbidden game-data/APK/archive/private-key paths among 260 repository candidates and zero forbidden entries among 312 production-AAR entries | Ongoing discipline | Tracked-file and artifact scans remain clean at every gate |
| Goal 10: reproducible signed releases | 7 | BLOCKER | Final-identity run `33305033556` proves the stable GitHub-secret-backed signer, certificate pin, non-debuggable APK, in-job redownload, and independent fresh-download verification; no same-commit repeat, aligned release tag, published release, install, or update proof exists | Remaining Task 17 plus all device gates and Task 18 | Rebuild one exact commit twice, then fresh-download/hash/signature/install/update verification passes |
| Non-goal: arbitrary cross-game mod binary compatibility | Boundary | COMPLETE | Design explicitly requires separate per-game compilation | None | No shared compiled game-dependent patch DLL exists |
| Non-goal: one compiled patch DLL for both games | Boundary | COMPLETE | Source-sharing boundary is explicit | None | Build manifests show separate profile patch outputs |
| Non-goal: mixing builds or operating systems | 2 | COMPLETE | Exact-manifest validation rejects platform, identity, hash, and semantic-action mixing; dedicated synthetic cases return `MIXED_BUILD` | None | Full `HollowKnightSourceValidatorTest` suite passes |
| Non-goal: ship or publish user game content | Every stage | COMPLETE | No game content is tracked at baseline | Ongoing scans | Repository/APK scans remain clean |
| Non-goal: retain native GLES3/EGL as primary renderer | 6 | NOT-STARTED | Shared Vulkan renderer work has not begun | Renderer extraction | Runtime evidence identifies Unity Vulkan/multi-display path |
| Non-goal: emulator as Thor proof | Boundary | COMPLETE | Evidence policy is explicit | None | Device-only rows cite serial `bfa98654` evidence |
| Non-goal: add x86-64 production pipeline | Boundary | COMPLETE | First implementation remains ARM64-only | None | No production x86-64 native artifacts are introduced |
| Architecture: dedicated launcher and one cold Unity process | 4 | BLOCKER | Android instrumentation proves the lab `:game` cleanly exits, its PID disappears, and the next profile receives a different PID; production Unity switching remains unproven | Parent Task 11 | ARM64 live-process mismatch is rejected without timeout |
| Architecture: launch request contains only profile ID | 4 | COMPLETE | `ProductionLauncherRuntimeTest` proves the explicit game intent contains exactly one extra, the registered profile ID; launcher uses the selected runtime to create it | None for the shared contract | Intent contains one profile ID and no path, credential, or artifact extra |
| Architecture: launcher settings are profile-scoped | 4 | COMPLETE | `ProfileSettingsStore` namespaces settings by registered profile, adopts legacy settings into Silksong exactly once, and never exposes them to Hollow Knight; 3 focused tests cover isolation/adoption/reset | None | Both profiles retain independent values and resetting one preserves the other |
| Architecture: alternate runtime requires exact lab identity | Lab | COMPLETE | `LauncherRuntimeProvider` defaults to `ProductionLauncherRuntime`, requires both exact package `io.github.darkaxt.dualsouls.emutest` and explicit manifest class metadata for an alternate, and fails closed on absent/missing/unassignable classes; 6 provider tests pass | None | Focused provider suite passes and production package cannot request the fake runtime |
| Architecture: exact declarative game profiles | 1–3 | COMPLETE | Registry tests plus the 1,748-entry semantic manifest pin current versions, platforms, Unity versions, actions, ownership, sizes, and hashes | Toolchain/build fields remain later milestones | Registry, manifest-asset, and real-source integration tests pass |
| Architecture: profile owns validation/toolchain/conversion/patch/save/features | 2–6 | BLOCKER | Profiles now own identity, source layout, exact manifest, Unity toolchain, conversion action, storage, selection metadata, separate patch assets/assemblies, and optional save-I/O compilation. Skin, mod, renderer, and real lifecycle adapters remain incomplete | Tasks 10–15 | Each remaining field/adapter is linked to its owning milestone test |
| Architecture: unknown versions fail closed | 2 | COMPLETE | Exact source validation returns `UNKNOWN_VERSION` for wrong app version, build revision, Unity version, and manifest identity with precise diagnostics | None | Full `HollowKnightSourceValidatorTest` suite passes |
| Architecture: toolchains shared by content hash/version | 3 | COMPLETE | `UnityToolchainRegistryTest` passes 6 cases for exact 6000.0.50f1/6000.0.61f1 resolution, fail-closed lookup, descriptor-hash roots, staged mismatch eviction without neighbor mutation, path containment, and `classes.jar`-hash dex identity; `docs/verification/unity-toolchains-2026-08-31.md` records primary metadata and derived SHA-256 values | None for the host registry; legacy global-cache adoption remains in the device procedure | Wrong hash cannot affect another toolchain directory |
| Architecture: profile path construction is isolated and contained | 1 | COMPLETE | `ProfilePathsTest` proves distinct roots, normalized containment, traversal rejection, absolute-ID rejection, and sibling-prefix safety | None | Targeted ProfilePaths test suite passes |
| Architecture: existing game pipeline uses only profile-scoped storage | 1 | BLOCKER | Task 4 routes setup, download, build, package, reset, shell libraries, and logs through `profiles/silksong`; the catalog link alone uses unique compact namespace `p/ss` to fit its fixed 56-byte field. Ten Robolectric regressions and the APK build pass, but the installed legacy generation has not been adopted. The final fork package `io.github.darkaxt.dualsouls` and signer `324b3a3e…8c38` can coexist with the upstream package/signer, but no data adoption has been exercised | Controlled one-time preservation/adoption into the non-colliding fork package | Install the fork-signed APK through the preservation procedure, prove legacy roots move only to the Silksong profile, then launch without touching the Hollow Knight root |
| Architecture: source depot remains untouched | 2–3 | COMPLETE | Repeated read-only current-source inventories retain tree SHA-256 `1319a0ea…daa42f8`; the generator writes only the disjoint manifest path | None for validation/generation | Before/after current-source content and timestamp manifests agree |
| Architecture: atomic generation publication and rollback | 3 | COMPLETE | Twelve publisher tests and three coordinator tests prove manifest-last sealing, all-file SHA-256, APK/OBB reopening, immutable targets, previous-current retention, exact-job discard, and post-move revalidation. Attempt 4 publishes and independently hashes the exact APK/OBB generation on Thor; the Java shell resolves only `current` and never mutates a published generation | None | Injected stage failure and payload mutation retain the prior current generation |
| Architecture: shared provisioning state machine | 3 | COMPLETE | `ProfileBuildCoordinator` runs eight sealed stages in fixed order and emits structured stage/index/count progress; production setup supplies the existing source, toolchain, patch, IL2CPP, native, content, and verification operations for the selected profile | Task 10 supplies the classic profile operations | Coordinator order and production compile tests pass |
| Architecture: cooperative cancellation, no elapsed-time cancellation | 3 | COMPLETE | Coordinator and payload hashing check coroutine cancellation between stages and file batches, discard only the owned staging job, and run the final atomic publication non-cancellably; no elapsed-time cancellation was added | None for host behavior | Cancellation test preserves the current pointer and sibling staging job |
| Architecture: Silksong Addressables conversion preserved | 1 | COMPLETE | Fork-signed run `33428813976` updated the final package in place, correctly rejected its stale generation, rebuilt the exact `1.0.29980` source, atomically published verified generation `gen-92b911fd-0063-4a83-b8f1-a34ec3e893b6`, and reached Moss Grotto gameplay without changing the source hash | None for Silksong regression | Repeat preservation, repair and Play on `bfa98654` after patch changes |
| Architecture: Hollow Knight classic discovery by content | 2 | COMPLETE | Task 6 discovers extensionless serialized files by header, sorts numeric names within directories, binds exact sidecars, and emits deterministic hashes; live reparse-fixture coverage is a tracked deferral because project policy forbids symlinks | Non-symlink reparse integration fixture before promotion | 24 focused legal-synthetic tests pass, then the integration fixture rejects a reparse path |
| Architecture: every serialized file becomes Android/Vulkan | 2 | COMPLETE | The exact 1.5.12620 report proves all 1,005 manifest-selected serialized files transformed to Android target 13; 125 shaders retain Vulkan slices, with zero missing Vulkan and zero diagnostics | None for host conversion | Repeat current full report and output reopening |
| Architecture: built-ins replaced and sidecars preserved | 2–3 | COMPLETE | All 610 sidecars retain their exact hashes; Android `unity default resources` hash `32b37bf1…6f991` is installed and the game-owned `unity_builtin_extra` is transformed to Vulkan hash `edaead75…e7f1` | None for host assembly; device loading remains Goal 4 | Exact resource hashes and representative sidecar hashes match after assembly |
| Architecture: plugins/media are inventoried before conversion policy | 2–3 | COMPLETE | Current report distinguishes 118 managed DLLs to copy from four Linux native plugins to exclude and reports zero media files | None for inventory policy | Exact manifest contains 737 copy and four exclude actions |
| Architecture: classic output is resumable and Unity-readable | 2–3 | COMPLETE | Resume key `88047359…c85a` binds source/manifest/transformer identity. Attempt 4 reuses the retained tree, emits a 3,575,674,865-byte ZIP32 base and 1,603,506,295-byte main OBB with matching GUIDs, reopens and hashes both, then Unity mounts them through the title, opening sequence, and first playable room | None for the exact Unity split; multi-OBB size optimization is deferred | Repeat the exact-image split and first-room device launch |
| Architecture: shared patches contain no game-specific types | 5–6 | COMPLETE | The extracted tweak contracts/controller compile and pass 24 host tests without either game assembly; both profile patch assets stage the same sources | The Dual Souls composition port must preserve this boundary between shared contracts and resident-object adapters | Shared core compiles alone; both exact profile patch checks compile it beside their own adapter sources |
| Architecture: external mods resolve per profile and fail safely | 5 | BLOCKER | Build-time BepInEx DLL discovery/weaving now uses the selected profile's patch, IL2CPP, generation, report, and staleness roots; unsupported patches are reported without invalidating the base pipeline. Manifest compatibility, dependencies, deterministic order, per-profile enablement, and explicit safe launch are absent | Task 14 resolver and safe mode | Compatibility/dependency/cycle/disabled/compile-failure/safe-mode tests pass for both profiles |
| Architecture: one built-in Mods menu with typed per-game tweaks | 5–6 | IN-PROGRESS | Shared controller, fail-closed persistence, typed Silksong `1.0.29980` adapter, `PlayerPrefs` store, and framed overlay are implemented. Mods now opens only from the dedicated gear outside the bottom tab list. The signed Thor run proves the ARM64 renderer and first-run `dualsouls.mods.silksong.master=0`. Native offsets remain prohibited | Signed rendering/touch of the redesign, effects, restart persistence, Hollow Knight adapter/isolation, and parity rows remain | Redesigned gear/overlay passes the signed Thor visual/touch/effect/restart matrix |
| Architecture: skin scanner treats immediate children as packs | 5 | NOT-STARTED | Skin scanner has not begun | Manifest schema | Multiple valid siblings survive one invalid sibling |
| Architecture: duplicate skin IDs require safe resolution | 5 | NOT-STARTED | Skin registry has not begun | Tree hashes | Identical/different duplicate tests pass without flattening |
| Architecture: one active skin per game | 5 | NOT-STARTED | Activation store has not begun | Profile storage | Independent selection persists for both profiles |
| Architecture: death selects; stable respawn applies | 5 | BLOCKER | `SkinRotationStateMachine` passes 6 host tests and the lab game calls the same death/stable-respawn API; real Hollow Knight and Silksong lifecycle/apply adapters are absent | Tasks 12–14 lifecycle adapters | Ordered/shuffled/rollback state tests and both device checks pass |
| Architecture: shared Unity Vulkan multi-display renderer | H1–H6 | IN-PROGRESS | The minimum shared seam is proven, and Hollow Knight H2 is source-complete on it: 24 focused contracts, 58 shared tests, 102 Python tests, both exact compiles, and a 222,720-byte exact patch pass. Signed Thor evidence proves display-1 HUD routing, primary cleanup, dark blurred backdrop, safe panes, bounded ornaments, lower-controller page switching, selection/prompt, and pause presentation | TMP tab text still has valid geometry but no pixels; controlled/static-state overlay and lifecycle rows, H3–H5, and physical detach/true single-display proof remain | Complete Hollow Knight companion passes display 1, touch, lifecycle, fallback, Mods, and skins before H6 extraction |
| Architecture: game adapters supply screens/state only | H6, S1 | NOT-STARTED | Hollow Knight adapter and later shared boundary are absent | Accepted H5 behavior | Shared runtime references no game-specific types and the H5 regression remains unchanged |
| Architecture: Dual Souls composition port with semantic Silksong extensions | H2–H6, S1 | IN-PROGRESS | The rejected prototype path is dormant and Silksong source work is preserved. The exact pinned Hollow Knight composition is source-complete/device-partial through direct display; Hollow Knight H5/H6 precede shared contracts and Silksong resident adapters | Close H2–H6, then `DsPort*` successor work | No production `DsShell` path remains; live Hollow Knight first passes the full matrix; then both exact compiles and side-by-side captures satisfy the same composition and behavior matrix |
| Architecture: promotional launcher art stays out of in-game themes | 3, 6 | IN-PROGRESS | Launcher resources remain isolated; the live Silksong surface used only runtime/procedural UI and no supplied promotional image | Resident ornament resolution, deterministic fallback audit, and Hollow Knight adapter remain | Resource audit finds promotional art only in launcher/shortcut resources and resident game art behind adapters |
| Architecture: single-display fallback remains usable | H1/H5 | IN-PROGRESS | Shared presence/loss/reactivation and fallback decisions pass executable host tests. Thor's two internal panels remain registered by its composer after the lower DRM connector is forced disconnected, and existing x86-64 AVDs cannot execute the ARM64 Unity player | ARM64 single-display target or composer-level Thor removal event by H5 | Host decision tests remain green and a native device fallback smoke passes |
| Branding: unchanged vertically split combined identity in Android launcher shapes | 7 | COMPLETE | `docs/verification/icon-2026-08-30.md` pins the supplied composition hash and reviews four masks; `docs/verification/product-identity-2026-08-30.md` proves package `io.github.darkaxt.dualsouls`, label `Dual Souls`, final artifact naming, and zero alpha/premultiplied differences across all 15 signed-APK PNGs in run `33305033556` | None for the Task 16 identity/icon slice | Source hash, circle/squircle/rounded/OEM preview, AAPT compile, manifest identity, signer, and rendered packaged comparisons pass |
| Recovery: validation never writes to source | 2 | COMPLETE | Current-source scans and manifest generation use disjoint report/asset paths and retain the exact source tree hash | None | Before/after content and timestamp manifests agree |
| Recovery: generated artifacts stage and verify before publish | 3 | COMPLETE | Production outputs are routed into an owned generation job; the manifest is written last, every payload is hashed, archive entries are reopened, the moved tree is revalidated, and only then is `current` atomically replaced | None for host implementation | Injected production-stage failures never update the current pointer |
| Recovery: resume/discard exact staging job only | 2–3 | COMPLETE | Expensive external conversion intermediates remain resumable, while coordinator interruption discards only its uniquely assigned internal generation job and preserves a sibling, source tree, and previous generation | None for host implementation | Interruption test preserves previous generation and unrelated jobs |
| Recovery: unsupported/corrupt input fails before replacement | 2–3 | COMPLETE | Exact source/converter validation, payload SHA mismatch, unlisted payload, corrupt ZIP, and corrupt OBB checks all fail before pointer replacement; tests prove previous-generation retention | None for host implementation | Error matrix retains the previous generation |
| Recovery: logs identify profile/stage/artifact without credentials | 3 | NOT-STARTED | Structured logging has not begun | Coordinator | Redaction and structured-event tests pass |
| Recovery: reset is profile-scoped | 3–4 | BLOCKER | `BuildReset` now clears the selected profile's production staging and published generations in addition to legacy outputs; Android lab instrumentation proves the Silksong current pointer/generation survives a Hollow Knight reset. Saves, credentials, legacy adoption, and ARM64 device coverage remain open | Parent Task 11 and controlled device procedure | Full production reset matrix preserves the other game, shared library, saves, credentials, and previous valid generation |
| Recovery: encrypted Steam credential boundary is retained | 1–4 | BLOCKER | Task 4 does not change `TokenStore`, and the fork key is secured outside source plus GitHub secrets, but the new non-colliding package does not inherit the upstream package's encrypted login and no preservation/adoption proof exists | Controlled migration into the final fork package | Existing login remains usable after adoption and the next fork-signed update without plaintext storage or logging |
| Host test: profile registry | 1 | COMPLETE | `GameProfilesTest` passes for registry, current/compatibility versions, and unknown IDs | None | Targeted GameProfiles test suite passes |
| Host test: selected-profile persistence | 1 | COMPLETE | `SelectedGameStoreTest` covers default, recreation, unknown stored IDs, and unregistered-profile rejection | None | Targeted SelectedGameStore suite passes |
| Host test: exact source-manifest validation | 2 | COMPLETE | Kotlin covers all six statuses, semantic hash/action tampering, extra files, and the real 1.5.12620 report with no skip | None | Full Android host suite passes 28/28 with real evidence variables set |
| Host test: platform/version rejection | 1–2 | COMPLETE | Windows platform, wrong app version, wrong build revision, wrong Unity version, missing files, hash mixing, and missing Vulkan fail closed | None | Targeted `HollowKnightSourceValidatorTest` passes |
| Host test: classic discovery/sidecars | 2 | COMPLETE | 24 focused Task 6 tests cover discovery, exact sidecars, manifests, conversion, census, resume, aliases, and command failures; live reparse coverage is tracked separately | Non-symlink reparse integration fixture before promotion | `dotnet test ... --filter FullyQualifiedName~ClassicPlayerTreeTests` passes 24/24 |
| Host test: Vulkan coverage/target rewrite | 2 | COMPLETE | Six synthetic tests cover required/optional missing Vulkan, target 13, blob selection, multi-chunk offsets, `.part` reopen, and header-based extensionless input | None | `dotnet test tools/bundle-surgery-tests/BundleSurgery.Tests.csproj -c Release --no-restore` passes 6/6 |
| Host test: atomic generation/recovery | 3 | COMPLETE | Twelve `GenerationPublisherTest` cases and three `ProfileBuildCoordinatorTest` cases pass, covering hashes, APK/OBB reopening, mutation/unlisted-file rejection, ordered stages, injected failure, cancellation, immutable target, exact discard, cross-profile clearing, and path rejection | None for the shared host API | Both focused build-package test classes pass |
| Host test: mod/skin manifests and safe mode | 5 | BLOCKER | Mod baseline tests now cover recursive discovery, config exclusion, independent profile stamps, non-destructive gates, clean weaver staging, selected-profile setup wiring, and Hollow Knight/BepInEx registration. Manifest/dependency/safe-mode and all skin-library suites remain absent | Tasks 12–14 | Compatibility/collision/dependency/safe-mode and skin suites pass |
| Host test: death-to-respawn rotation | 5 | COMPLETE | 6 pure shared state-transition tests prove different-skin selection, pending stability, stable-respawn commit, apply-failure rollback, disabled/single-pack behavior, and reset | None for the shared host API; real adapters remain a parent blocker | `SkinRotationStateMachineTest` passes |
| Host test: launcher/settings/reset isolation | 1–4 | BLOCKER | The combined host gate passes 101 tests with 99 passed, zero failures/errors, and two environment-gated skips. Contracts prove exact first-scene selection, APK/OBB paths and GUID equality, classic readiness requiring its OBB, and corrupt-OBB rejection; attempt 4 supplies the real split/mount proof. Three Android instrumentation tests prove fake-runtime generation/recovery/reset and cold switching, while real ARM64 two-game switching remains absent | Parent Task 11 | Full production generation and process-switch matrix passes |
| Emulator: Robolectric covers fakeable UI/state behavior | 1–5 | COMPLETE | The host runner loads real Android resources; the Task 16 gate passes 38 tests with 37 passed and one intentional skip, including the final launcher label contract | Pinned Gradle/Android player module | `:app:testDebugUnitTest` passes on the host |
| Emulator: x86-64 is not native evidence | Boundary | COMPLETE | Design and plan explicitly exclude it as release proof | None | Traceability cites no x86 native/device claims |
| Emulator: dedicated fake-runtime integration instance | Lab | COMPLETE | `docs/verification/emulator-lab-2026-08-30.md` records the exact AVD/serial/API/ABI/Thor exclusion and the Task 9 rerun: 84 app host tests, six fake-runtime host tests, three green Android tests, rendered fake-runtime UI, profile persistence, atomic recovery/reset, storage, and old/new PID evidence | None for the emulator lab; ARM64 remains separate | Repeat `tools/emulator/Test-DualSoulsLab.ps1` without claiming native proof |
| Emulator: production and release isolation | Lab | COMPLETE | The post-parent-sync release AAR/runtime-dependency build passes; unused duplicate provisioning is removed; release guards require the exact production filename/package and prove the workflow never invokes the lab module | None for the emulator specification | Full emulator, Gradle, and 21-test CI gates pass together |
| Device gate 1: clean provisioning for both Linux sources | Release | COMPLETE | Hollow Knight's exact Linux `1.5.12620` source and Silksong's exact Linux `1.0.29980` source each complete source-to-ready on the Thor. The `Games` Hollow Knight folder is Windows-only and remains excluded | None for per-profile provisioning; same-installation adoption is Goal 1 | Clean source-to-ready run passes per profile |
| Device gate 2: exact toolchain and on-device IL2CPP | Release | COMPLETE | Hollow Knight converts 169 assemblies to 803 C++ and 180 C units and publishes a 267,628,408-byte ARM64 library. Silksong run `33428813976` converts 955 C++ and 200 C units, links 1,559 objects, and publishes hash-recorded 316,375,952-byte `libil2cpp.so` | None for the two exact toolchain builds | Device logs and installed-generation hashes prove the same exact components for both profiles |
| Device gate 3: menu and representative gameplay | Release | COMPLETE | Hollow Knight reaches its first controllable room; fork-signed Silksong reaches Moss Grotto with the expected `1.0.29980` HUD and direct lower display | None for basic gameplay | Both profiles enter gameplay |
| Device gate 4: rendering/audio/video/input/save/resume | Release | BLOCKER | Both profiles render and accept primary input in gameplay; Hollow Knight initializes AAudio, decodes VP8/Vorbis and creates slot 1. Silksong input skipped the opening sequence and reached gameplay, but its save/reload/resume, audio evidence and replacement lower-panel physical touch remain open | Save/reload/resume, audio capture, Task 14 input adapter, and physical lower touch | Recorded functional matrix passes |
| Device gate 5: representative scene coverage | Release | BLOCKER | Hollow Knight title, opening cinematic and first room pass. Silksong title, opening sequence, Moss Grotto gameplay and lower Map shell pass. Boss, effects-heavy and dependency-heavy scenes remain untested for both | Stable gameplay and later milestones | Menu/map/boss/effects/dependency-heavy checks pass |
| Device gate 6: single and Thor dual display | Release | IN-PROGRESS | Fork-signed Hollow Knight now runs its source-complete/device-partial Dual Souls companion on the Thor's second physical panel; Silksong's prior rejected prototype remains transport-only evidence | Close Hollow Knight H2–H5, true single-display fallback, shared extraction, and Silksong adaptation | Both modes pass on `bfa98654` for both profiles with the production port |
| Device gate 7: display-specific touch | Release | BLOCKER | Production code fences by Unity Input System `displayIndex`; host geometry tests pass; and the real lower controller's display-associated `fts_ts_3` kernel event path selects Inventory, Charms, Map, and an Inventory item without affecting primary input. High-level ADB display injection is not counted as proof | Physical-finger confirmation plus gear/modal/gesture rows after their Hollow Knight stages | Physical gear, every tab, modal controls, drag and pinch operate only the lower display while primary input remains unaffected |
| Device gate 8: base/safe/mod/skin/rotation matrix | Release | NOT-STARTED | Feature milestones have not begun | Mods and skins | Full feature matrix passes for both profiles |
| Device gate 9: switch only after old Unity exits | Release | NOT-STARTED | Switching has not begun | Cold-process model | Both directions prove old PID gone |
| Device gate 10: update/rollback without loss | Release | NOT-STARTED | Atomic generation/update logic is host-complete, but the device matrix has not begun | Controlled ARM64 adoption | Update and rollback preservation matrix passes |
| Release gate: repository/APK contain no proprietary content | 7 | BLOCKER | The Task 16 scan found zero forbidden paths among 178 tracked files and zero game-content/private-key matches among 257 entries in the final-identity dry-run APK; the completed dual-game release candidate does not yet exist | Final artifact | Repeat tracked-file and APK scans against the release candidate |
| Release gate: clean CI builds and tests | 7 | BLOCKER | Release run `33305033556` builds the final identity from a clean checkout; the post-parent-sync local CI suite now passes 19 tests including profile-mod isolation and exact lab exclusion, but the planned push/PR `check.yml` and complete non-proprietary suite are absent; Node.js runtime and Docker platform lint annotations remain tracked Task 17 deferrals | Remaining Task 17 | Clean-checkout push/PR and dry-release workflows pass without runtime/platform annotations |
| Release gate: signing secrets stay out of source/logs | 7 | BLOCKER | Four GitHub repository secrets are configured; the PKCS#12 key remains outside source and is mounted read-only from runner temp, while the committed pin plus CI/fresh-download checks prove signer `324b3a3e…8c38` for `io.github.darkaxt.dualsouls`. No key appears in tracked files or APK entries | Controlled device adoption plus a second fork-signed build | Two sequential fork-signed APKs use the pinned certificate and update in place without exposing key material |
| Release gate: numeric identifiers all agree | 7 | BLOCKER | Run `33305033556` aligns package `io.github.darkaxt.dualsouls`, label `Dual Souls`, `versionName=1.0.3`, `versionCode=10003`, and filename `DualSouls-1.0.3.apk`. The inherited `v1.0.3` tag still points to upstream commit `d504275`, and no release title exists | Task 17 version/tag correction plus Task 18 | A new release candidate's APK version/code, tag target, release title, and filename all identify the same commit and version |
| Release gate: downloaded APK is reverified and exercised | 7 | BLOCKER | GitHub and an independent local download both hash- and signature-verify run `33305033556` artifact `687765af…c7a`; the dry run deliberately published nothing and no install, update, launcher, or game smoke was performed | Controlled adoption, completed application, Task 18 | Fresh published download passes hash/signature/update and both-profile smoke on Thor |
